
  # Generate SafeMining VR App (copia)

  This is a code bundle for Generate SafeMining VR App (copia). The original project is available at https://www.figma.com/design/MzoEubyAuEzyzgodMRHYWu/Generate-SafeMining-VR-App--copia-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  